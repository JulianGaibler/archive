apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adminer
  namespace: archive
spec:
  tls:
  - hosts:
    - archive.arz.berlin
    secretName: archive-tls
  rules:
  - host: archive.arz.berlin
    http:
      paths:
      - path: /adminer
        pathType: Prefix
        backend:
          service:
            name: adminer
            port:
              number: 80
